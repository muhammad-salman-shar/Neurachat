name: Build Android APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: "22"
        cache: 'npm'

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: "21"

    - name: Install Dependencies
      run: npm install --legacy-peer-deps

    - name: Next.js Build and Export
      run: npm run build

    - name: Create www folder for Capacitor
      run: |
        mkdir -p www
        if [ -d "out" ]; then
          cp -r out/. www/
        else
          echo "<html><body>App</body></html>" > www/index.html
        fi

    - name: Add Android Platform
      run: rm -rf android

    - name: Fresh Cap Add
      run: |
        rm -rf android
        npx cap add android

    - name: Copy Custom AndroidManifest
      run: cp .github/manifests/AndroidManifest.xml android/app/src/main/AndroidManifest.xml

    - name: Sync Android
      run: npx cap sync android

    - name: Build Android App
      run: |
        cd android
        chmod +x ./gradlew
        ./gradlew assembleDebug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: NeuraChat-Debug-APK
        path: android/app/build/outputs/apk/debug/app-debug.apk
